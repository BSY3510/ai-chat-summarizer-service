version: '3.8'
services:
  mysql-local:
    image: mysql:8.0
    container_name: chatbot-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: local_password
      MYSQL_DATABASE: chatbot_db